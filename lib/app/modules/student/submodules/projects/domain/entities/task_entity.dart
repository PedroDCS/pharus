class TaskEntity {
  final String title;
  final bool isComplete;
  final String description;
  TaskEntity({
    required this.title,
    required this.isComplete,
    required this.description,
  });
}
