class TaskEntity {
  final String name;
  final bool isComplete;
  final String description;
  TaskEntity({
    required this.name,
    required this.isComplete,
    required this.description,
  });
}
